import { createTheme } from "@material-ui/core";

export const theme = createTheme(
    {
        palette: {
            primary: {
                main: "#3f9994",
                light: "skyblue"
            },
            secondary: {
                main: "#17a3a1"

            },
            otherColor: {
                main: "999"
            }
        }
    }
)
